import {  StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    Pressable: {
        backgroundColor: '#000',
        width: 50,
        height: 50,
        borderRadius: 50,
        display: 'flex',
        alignItems: 'center',
        position: 'absolute',
        right: 30,
        bottom: 30,
    },

    Text: {
        color: '#fff',
        fontSize: 40,
    }
});